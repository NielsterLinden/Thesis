# @package _global_
# Base model size (default) - full capacity

classifier:
  model:
    dim: 256              # Model dimension
    depth: 6              # Number of transformer layers
    heads: 8              # Number of attention heads (head_dim = 32)
    mlp_dim: 1024         # MLP hidden dimension (4x dim)
