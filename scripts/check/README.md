# Check Scripts

Validation and verification scripts. Run from project root.

| Script | Purpose |
|--------|---------|
| `check_ids.py` | Verify H5 token IDs match Hydra config |
| `check_logging_import.py` | Lint: ensure logging import present |
| `check_run_completeness.py` | Validate run dirs (facts, checkpoints) |
