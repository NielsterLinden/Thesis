# Agent reference material (local only)

Put papers, PDFs, notes, or other files here for agents to read on this machine. Everything in this directory except this `README.md` is **gitignored** and will not be committed.

Tip: In chat, point the agent at a path under `agent_reference/` (for example `agent_reference/papers/foo.pdf`) so it knows to open it.
